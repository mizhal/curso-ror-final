<article class="micro-offer-card">
	<%= image_tag offer.main_photo(:micro) %>
	<h1><%= link_to offer.name, 
		accommodation_offer_path(offer.accommodation_id, offer.id) %></h1>
	<p class="from">
		<%=t '.from' %>
		<%= offer.price %>€
		(<%=t '.vat_included' %>)
	</p>
</article>
